import {PeselValidationDirective} from './pesel-validation.directive';

describe('PeselValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new PeselValidationDirective();
    expect(directive).toBeTruthy();
  });
});
